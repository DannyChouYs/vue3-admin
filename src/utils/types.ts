export interface registerType {
    name: string;
    email: string;
    password: string;
    password2: string;
    identity: string;
}

export interface loginType {
    email: string;
    password: string;
}
